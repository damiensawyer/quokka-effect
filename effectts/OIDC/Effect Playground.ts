import { Effect, Data, pipe, Context } from "effect";

// class Logger extends Context.Tag("Logger")<Logger, {
//     readonly Log: (sql: string) => void
// }> () { }

// var s = Logger.of({Log:(s)=>{console.log(s);
// }});

